export {
  logoQuerySchema,
  updateSettingsSchema,
  getSettingsSchema,
  type UpdateSettingsInput,
} from '@aifc/contracts';

// NOTE: schemas live in shared/src/features/settings/schemas.ts (single source of truth).
